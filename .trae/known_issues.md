# 已知问题记录 (Known Issues)

本文件用于记录历史上出现过的问题、原因及解决方案。Trae 在执行代码修改前会参考此文件以避免重复错误。

| 问题描述 | 发生原因 | 解决方案 | 状态 | 记录时间 |
| :--- | :--- | :--- | :--- | :--- |
| Login.vue 表单提交重复触发 | 未对提交按钮进行 loading 状态控制或防抖处理 | 增加 `loading` 状态控制，并在提交前校验 | 已修复 | 2026-05-24 |
| 树右键依赖筛选结果失效 | 筛选后的树节点引用未正确更新 | 统一使用 `orgUnit` store 中的响应式数据进行操作 | 已修复 | 2026-05-24 |
| 上级组织编辑禁用冲突 | 编辑时未排除自身导致父子节点环路判断错误 | 在上级组织下拉列表中排除当前正在编辑的组织及其子组织 | 已修复 | 2026-05-24 |
| level 字段类型不一致 | 后端 DTO 为 int，前端有时按 string 处理 | 统一在前后端使用数字类型（0-4），并在前端 API 层进行强制转换 | 已修复 | 2026-05-24 |
| 待办中心跳转流程中心失败 | 路由路径不匹配（`/workflow/processes` vs `/workflow/process`） | 统一前端路由为 `/workflow/processes` 并修复 `TodoCenter.vue` 中的跳转逻辑 | 已修复 | 2026-05-24 |
| 薪资状态变更后前端不刷新 | 异步请求完成后未调用 `loadData` 或 `refresh` 方法 | 在所有 Action 方法（Approve/Pay/Rollback）后增加数据加载逻辑 | 已修复 | 2026-05-24 |
| 工作流代理解析 `null` 响应时报错 | `WorkflowCenterResponseParser` 默认假设根节点是 JSON Object，遇到下游返回 `null`/空响应时直接调用 `TryGetProperty` | 读取 `message` / `data` 前先校验 `RootElement.ValueKind == Object`，否则降级返回 `null` | 已修复 | 2026-05-24 |
| 联调时频繁出现 404/旧代码未生效 | 后端服务修改后未统一重启，旧进程残留占用端口，前端代理继续连到旧服务 | 增加统一的开发环境启动/重启/停止/状态检查脚本，启动前清理旧进程并通过 `/health` 做验证 | 已修复 | 2026-05-25 |
