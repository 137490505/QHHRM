# 供应商门户解决方案备选计划

## 一、目标背景

当前系统如果未来需要开放给供应商使用，让供应商自行维护或提交三方员工信息，并查看本供应商名下员工的当月产值、账单汇总或相关协同数据，需要优先考虑以下问题：

- 外部账号与内部账号的隔离
- 供应商只能看到本供应商的数据，不能越权查看其他供应商或内部员工数据
- 敏感字段最小化暴露，避免身份证、手机号、薪资、社保、公积金等数据泄露
- 对外接口具备登录安全、操作审计、限流和告警能力

本文件先输出解决方案备选计划，后续正式开发时再进一步细化表结构、接口和排期。

## 二、方案备选

### 方案 A：在现有 HRMS 内增加供应商门户

#### 方案说明

在当前 `HRMS` 系统内新增一套供应商角色、菜单、页面和接口，供应商通过现有系统登录，但只看到供应商专属菜单，例如：

- 我的三方员工
- 我的当月产值
- 我的账单汇总
- 我的资料维护

后端在现有服务中新增供应商专用接口或在现有接口中增加供应商场景判断。

#### 优点

- 开发速度最快
- 可以复用现有用户、供应商、员工、工时、账单等数据结构
- 前后端改动集中在当前项目，便于快速验证业务

#### 风险

- 和内部系统共前端、共后端、共数据库，隔离边界最弱
- 如果继续复用内部 DTO 或内部接口，容易出现越权和敏感数据泄露
- 后续功能越多，权限和数据范围判断会越来越复杂

#### 适用场景

- 仅少量供应商使用
- 需求以只读查询为主
- 目标是短期快速上线试运行

### 方案 B：独立供应商门户，复用现有后端能力

#### 方案说明

单独开发一个“供应商门户”前端应用，面向外部供应商使用；后端仍在当前系统内新增一组 `vendor/*` 或 `supplier-portal/*` 专用接口。内部 HRMS 页面和外部门户页面分离，但底层可复用当前数据库和业务服务。

示例能力：

- 供应商登录
- 查看本供应商名下三方员工
- 查看本月产值汇总
- 查看账单汇总
- 提交员工资料

#### 优点

- 安全边界比方案 A 清晰
- 外部门户页面与内部后台完全分开，减少误暴露内部菜单的风险
- 可以单独设计 DTO，只返回外部需要的数据
- 便于后续逐步扩展为独立服务

#### 风险

- 开发量中等
- 需要补一套对外认证、数据权限、审计、限流和异常访问监控

#### 适用场景

- 既要兼顾安全，又希望控制开发成本
- 未来有继续扩展供应商协同需求的可能

### 方案 C：独立供应商中心服务

#### 方案说明

单独建设“供应商中心”或“外部门户服务”，采用独立服务、独立前端、独立认证边界。内部 HRMS 只作为业务主系统，对外通过专用 BFF、网关或同步服务输出已筛选后的业务数据。

外部门户后续可以承载：

- 供应商账号管理
- 员工资料提交与审核
- 月度产值查询
- 对账单查看与确认
- 附件上传
- 消息通知
- 审批协同

#### 优点

- 安全隔离最好
- 便于平台化发展
- 可独立扩展部署、限流、审计和监控
- 更适合长期外部协同使用

#### 风险

- 开发量最大
- 运维复杂度更高
- 初期投入成本较高

#### 适用场景

- 供应商数量较多
- 外部协同场景会持续扩展
- 对数据隔离和安全合规要求高

## 三、推荐顺序

### 短期推荐

优先推荐 `方案 B：独立供应商门户，复用现有后端能力`。

#### 推荐原因

- 比方案 A 更安全，能避免外部用户直接进入内部后台
- 比方案 C 成本更可控，适合当前阶段落地
- 后续如果需求扩大，可平滑演进到方案 C

### 过渡方案

如果只是想先让少量供应商查看“当月产值”，也可以短期采用方案 A，但必须从一开始就按供应商专用接口和专用 DTO 的思路设计，避免后续返工。

### 长期演进

如果未来要做完整供应商协同平台，建议最终演进到方案 C。

## 四、所有方案的共同前提

无论最终选哪种方案，都建议遵循以下基础原则。

### 1. 账号绑定供应商

每个外部账号必须明确绑定一个 `SupplierId`。

后端查询数据时必须根据当前登录账号绑定的 `SupplierId` 自动限域，不能让前端通过传参决定查询哪个供应商的数据。

### 2. 数据最小化返回

外部接口不能直接复用内部员工 DTO。

供应商侧只返回最小必要字段，例如：

- 员工编号
- 员工姓名
- 所属项目或产线
- 月份
- 工时汇总
- 合格产量
- 结算产值
- 状态

默认不返回以下敏感信息：

- 身份证号
- 手机号
- 邮箱
- 月薪
- 社保基数
- 公积金基数
- 合同细节

### 3. 对象级权限控制

不能只做菜单权限，还必须做对象级权限控制。

也就是说，即使供应商有“查看员工”权限，也只能查看自己供应商名下员工，不能通过篡改参数访问其他供应商的数据。

### 4. 审计与风控

所有外部关键操作都应记录审计日志，包括：

- 登录
- 查询敏感列表
- 导出
- 提交员工资料
- 修改联系方式
- 查看账单

同时建议增加：

- 登录失败次数限制
- IP 或账号限流
- 异常访问告警
- 导出频率控制

## 五、关于“供应商添加自己的三方员工”的建议

不建议让供应商直接写入正式员工主数据。

建议采用“申请制”流程：

1. 供应商提交员工资料
2. 进入待审核区
3. 内部 HR 或供应商管理员审核
4. 审核通过后进入正式员工主数据
5. 产值、工时、账单等业务再与正式员工绑定

### 原因

- 避免供应商直接污染主数据
- 便于控制字段完整性和格式校验
- 便于保留审核痕迹
- 后续可接入待办中心或流程中心

### 审批流程建议

建议这类“供应商新增三方员工资料”不要只做简单状态流转，而是直接纳入标准审批流程。

建议的基础流程如下：

1. 供应商提交员工资料
2. 系统生成一条申请单
3. 自动进入审批流程
4. 由内部 HR、业务负责人或供应商管理员逐级审批
5. 审批通过后写入正式员工主数据
6. 审批驳回后保留草稿与驳回意见，允许供应商修改后重新提交

建议后续优先复用当前项目已有的流程中心能力，不单独再做一套简化审批逻辑，这样后面更容易统一待办、流程、日志和审批记录。

### 审批节点建议

可按业务复杂度保留两种模式：

- 简版流程：供应商提交 -> HR 审核 -> 生效
- 标准流程：供应商提交 -> 业务负责人审核 -> HR 审核 -> 生效

如果未来还要控制费用、结算或编制占用，也可以继续扩展：

- 供应商提交 -> 业务负责人审核 -> HR 审核 -> 财务或经营复核 -> 生效

### 建议供应商可提交字段

- 姓名
- 工号或外部编号
- 性别
- 所属供应商
- 所属项目/产线/班组
- 入场日期
- 工种/岗位
- 备注

### 建议不允许供应商直接维护字段

- 薪资标准
- 社保、公积金基数
- 内部组织归属核心字段
- 系统角色权限
- 敏感身份信息的最终确认字段

## 六、建议的功能分阶段路线

### 第一阶段：只读查询版

目标：先让供应商安全地看数据，不开放写入。

建议功能：

- 供应商登录
- 我的员工列表
- 我的员工当月产值汇总
- 我的账单汇总

### 第二阶段：资料提交版

目标：让供应商提交员工资料，但不直接生效。

建议功能：

- 新增三方员工资料提交
- 提交记录列表
- 审核状态查询
- 驳回原因查看
- 提交流程发起
- 审批历史查看

### 第二阶段补充：流程可视化页面

建议在第二阶段同步提供“流程详情页”，用于查看当前申请正处于哪个审批节点，最好采用图形化标准显示。

建议页面能力如下：

- 显示流程名称、单号、申请人、申请时间、当前状态
- 图形化展示流程节点
- 高亮当前所在节点
- 标记已完成节点、进行中节点、驳回节点
- 展示每个节点的审批人、审批时间、审批意见
- 支持查看流程流转日志

建议优先采用标准 BPMN 风格或接近 BPMN 的流程图展示方式，这样和现有流程中心、流程设计器的输出更统一，也更方便后续直接复用流程定义和实例数据。

### 当前项目流程中心增强建议

除供应商门户场景外，建议当前项目的流程中心本身也补充一个“流程实例查看页”或在现有流程详情页中增加“流程图”标签页，用于查看审批流程当前走到哪个节点。

建议该页面面向当前项目所有审批流程通用，而不是只针对供应商新增员工流程，这样后续薪资审批、资料审批、合作伙伴审批等都可以复用同一套能力。

建议页面定位如下：

- 页面名称：流程实例详情页
- 页面目标：查看当前审批流程的实际流转状态
- 核心展示：以流程图形式展示完整审批链路
- 视觉重点：当前所在节点高亮显示
- 辅助信息：已完成节点、待处理节点、驳回节点、审批意见、处理时间

### 流程图页面建议能力

针对当前项目流程中心，建议流程图查看页至少具备以下能力：

- 根据流程实例加载对应流程定义图
- 根据实例运行时状态高亮当前节点
- 已通过节点使用完成态样式
- 当前节点使用高亮态样式
- 未到达节点保持默认态
- 驳回或终止节点使用异常态样式
- 支持节点点击后查看节点审批记录
- 支持同时展示流程基础信息和审批时间线

### 建议的页面结构

建议页面分为两块区域：

- 上方信息区：流程名称、业务单号、发起人、发起时间、当前状态、当前节点
- 下方流程图区：标准流程图展示，当前节点高亮，旁边或下方展示审批时间线

如果页面空间允许，也可以采用双栏结构：

- 左侧：流程图
- 右侧：审批记录、节点处理人、意见、处理时间、回调日志

### 图形化展示建议

建议优先采用标准 BPMN 图形展示，不建议只用简单步骤条替代。

推荐原因如下：

- 与当前项目流程设计器输出一致
- 用户更容易理解流程实际路径
- 后续并行网关、条件分支、驳回回退等复杂场景也能表达
- 可以直接体现“当前节点高亮”的核心需求

### 当前节点高亮规则建议

建议统一约定以下样式规则：

- 已完成节点：绿色或成功态
- 当前节点：蓝色高亮并带外发光或描边
- 待处理节点：灰色默认态
- 驳回节点：红色异常态
- 已跳过节点：浅灰弱化态

对于连线也建议同步体现状态：

- 已流转连线：高亮色
- 未流转连线：默认色
- 驳回路径：警示色

### 与现有流程中心的衔接建议

建议这项能力直接挂在当前项目现有流程中心模块下，实现路径可以有两种：

- 在流程中心列表点击实例名称后进入“实例详情页”
- 在现有流程详情抽屉中增加“流程图”标签页

如果后续审批场景会越来越多，建议优先采用独立“实例详情页”，这样页面承载能力更强，也更方便扩展审批记录、回调日志、催办记录和操作按钮。

### 建议增加的页面

围绕审批流程，建议供应商门户至少增加以下页面：

- 申请列表页：查看我提交的申请、状态、当前节点
- 申请详情页：查看申请内容、审批记录、驳回意见
- 流程图页面或流程详情卡片：图形化展示当前流程所在节点

如果内部管理端也需要同步查看，则内部可增加：

- 审批待办页
- 审批记录页
- 流程实例详情页

### 第三阶段：协同增强版

目标：形成供应商协同闭环。

建议功能：

- 账单确认
- 对账反馈
- 附件上传
- 导出带水印
- 消息通知
- 异常预警
- 图形化审批进度跟踪
- 流程催办与审批提醒

### 第四阶段：平台化版

目标：如果业务量扩大，升级为独立供应商中心。

建议能力：

- 独立服务
- 独立认证
- API 网关接入
- 更细粒度的安全审计
- 更完善的运维和监控

## 七、安全底线建议

在正式开放给供应商前，至少需要完成以下安全改造：

- 密码改为哈希存储，禁止明文密码
- 去掉默认弱密码
- 禁止前端保存明文密码
- 优先使用更安全的会话方案，例如 `HttpOnly Cookie`
- 所有外部接口补齐对象级权限校验
- 敏感字段脱敏或不返回
- 增加登录限流、失败锁定和异常访问告警
- 对查询、导出、批量接口做频率限制

## 八、初步结论

当前阶段建议保留以下三个正式备选：

### 备选 1：快速上线版

- 对应方案 A
- 适合短期验证
- 限制为只读查询

### 备选 2：标准实施版

- 对应方案 B
- 推荐优先采用
- 安全、成本、扩展性较平衡

### 备选 3：平台化版

- 对应方案 C
- 适合长期建设
- 安全和隔离最好

## 九、后续可继续细化的内容

等正式进入开发阶段后，可以继续拆解为以下交付物：

- 供应商门户功能清单
- 供应商账号模型设计
- 数据权限模型设计
- 接口清单
- 数据库表设计
- 前端页面结构
- 审批流程定义方案
- 流程图展示方案
- 开发排期
- 联调与测试方案

---

当前结论：

- 短期优先建议：`方案 B`
- 快速试运行可过渡：`方案 A`
- 长期平台化目标：`方案 C`
