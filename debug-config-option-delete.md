# Debug Session: config-option-delete

Status: [OPEN]

## Problem
- 症状: 系统配置 -> 下拉选项中执行删除后，刷新页面选项自动恢复。
- 预期: 删除后的选项应持久化，刷新页面后仍保持删除结果。

## Hypotheses
- H1: 前端删除后并未实际调用到保存接口，页面上的“删除成功”只是本地状态变化。
- H2: 前端调用了保存接口，但提交的 `options` 载荷仍包含被删除项或值类型不匹配，后端未真正删除。
- H3: 后端成功保存，但读取接口在返回时做了默认项回填或标准化，导致刷新后恢复。
- H4: 配置数据保存到了错误的分类键或被多条同键配置覆盖，刷新时读到的是旧记录。
- H5: 前端刷新加载链路在接口返回后又用本地默认配置覆盖了服务端结果。

## Evidence Plan
- 为前端删除动作、前端加载动作、后端读取接口、后端保存接口增加调试埋点。
- 复现一次删除并刷新，对比 delete/save/load 三段日志的入参与返回值。

## Progress Log
- 已建立调试会话，等待插桩与复现。
- 已在前端 `ConfigManagement.vue` 的删除/加载链路加入 A、E 埋点。
- 已在后端 `SettingsService.cs` 的读取/保存链路加入 B、C 埋点。
- 已启动本地调试采集器: `http://127.0.0.1:17777`
