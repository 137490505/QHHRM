# 待办中心与流程中心未实现功能清单

## 1. 文档说明

本文档用于梳理当前 HRMS 项目中 `待办中心（TodoCenter）` 与 `流程中心（ProcessCenter）` 已具备能力之外，仍未实现或尚未产品化落地的功能项，便于后续分阶段开发、排期和验收。

说明如下：

- 本清单基于当前项目代码现状整理，而非仅依据设计文档目标态。
- 已实现主链路包括：待办查询、详情、单条通过/驳回、批量通过/驳回、转交、催办、代理设置、KPI 统计，以及流程定义创建/发布、流程启动、流程推进、流程驳回、流程终止、重建当前待办等。
- 本清单重点关注“尚未实现”“只做了最小闭环但未生产化”“可以继续增强”的能力。

---

## 2. 当前已实现范围摘要

### 2.1 待办中心已实现

- 创建待办
- 我的待办查询
- 待办详情查询
- 单条通过
- 单条驳回
- 批量通过
- 批量驳回
- 单条转交
- 单条催办
- 代理规则新增与查询
- 基础 KPI 汇总

### 2.2 流程中心已实现

- 流程定义创建
- 流程定义发布
- 流程启动
- 流程实例列表查询
- 流程实例详情查询
- 处理待办后推进流程
- 驳回流程
- 终止流程
- 重建当前待办
- 流程结束后回调 HRMS 更新薪资审批业务状态

---

## 3. P0 级未实现项

以下功能属于“可运行但不适合正式上线”的核心缺口，建议优先处理。

### 3.1 持久化能力

- `TodoCenter` 仍使用内存态任务存储，服务重启后任务、日志、代理规则、催办记录会丢失。
- `ProcessCenter` 仍使用内存态流程定义、流程实例、流程历史存储，服务重启后流程状态会丢失。
- 尚未按设计文档落地 `MySQL` 表结构与仓储层。

建议补充：

- `TodoCenter` 的任务表、任务日志表、通知日志表、代理规则表、KPI 汇总表。
- `ProcessCenter` 的流程定义表、节点定义表、流程实例表、流程历史表、幂等事件表。

### 3.2 回调失败重试与补偿

- 当前跨服务调用失败后大多直接返回 `502`，没有自动重试。
- `TodoCenter -> ProcessCenter` 任务处理通知没有失败重试队列。
- `ProcessCenter -> HRMS` 业务回调没有失败重试与人工补偿入口。
- 没有失败回调重放机制。

建议补充：

- 回调失败重试表或消息队列。
- 最大重试次数控制。
- 人工补偿接口或后台页面。
- 回调重放审计日志。

### 3.3 死信与异常恢复机制

- 尚未接入 `RabbitMQ` 或其他消息中间件。
- 没有死信队列。
- 没有失败事件隔离与异步恢复能力。

建议补充：

- 回调消息异步投递。
- 死信队列和重试队列。
- 失败任务人工恢复能力。

### 3.4 超时任务调度

- 没有超时任务扫描器。
- 没有流程超时升级策略。
- 没有自动催办、自动同意、自动驳回等 SLA 策略。

建议补充：

- 定时任务扫描待办超时。
- 根据流程配置执行升级、催办或自动处理。
- 超时结果写日志并通知流程中心或业务系统。

### 3.5 正式通知服务

- 当前只有催办日志记录，没有真正发送消息。
- 尚未对接站内信、短信、邮件、钉钉、企业微信等通知渠道。

建议补充：

- 通知发送适配器。
- 通知模板管理。
- 渠道开关和失败重试。

---

## 4. P1 级待办中心未实现项

### 4.1 批量转交

- 当前仅支持单条转交，不支持批量转交。
- 实际审批场景中，审批人请假或临时调岗时，常需要批量转交。

### 4.2 批量催办

- 当前仅支持单条催办。
- 尚不支持对选中待办进行批量催办。

### 4.3 代理规则管理增强

- 当前只支持新增代理规则和查询代理规则。
- 尚未支持编辑代理规则。
- 尚未支持停用代理规则。
- 尚未支持删除代理规则。
- 尚未支持手动提前失效。

### 4.4 已办中心与更多任务视图

- 当前通过 `status` 查询可间接查看已完成/已驳回任务，但没有独立“我的已办”页面模型。
- 尚未支持“我发起的任务”“抄送我的任务”“我转交出的任务”“我代理处理的任务”等视图。

### 4.5 抄送/阅知能力

- 当前任务模型中没有抄送任务或阅知任务能力。
- 不能实现“仅通知不审批”的场景。

### 4.6 任务认领/签收机制

- 当前任务是直接指派，不支持待领取任务池。
- 尚未支持认领、退回待领取、转公共池等模式。

### 4.7 批量操作结果回显优化

- 后端已支持返回批量操作的部分失败明细。
- 前端尚未将成功/失败明细做成可视化弹窗。

建议补充：

- 成功数、失败数摘要。
- 失败任务列表。
- 每条失败原因展示。

### 4.8 KPI 统计增强

- 当前仅有总数、待处理数、已完成数、超时数、按时率。
- 尚未支持按审批类型、组织、人员、时间范围、平均耗时等统计维度。

---

## 5. P1 级流程中心未实现项

### 5.1 流程撤回

- 发起人提交流程后，当前不能执行“撤回”操作。
- 尚未支持“流程未被下一节点处理前允许撤回”。

### 5.2 驳回到指定节点/上一步节点

- 当前驳回会直接将流程置为 `Rejected` 终态。
- 尚未支持退回上一审批节点。
- 尚未支持退回到指定历史节点重新审批。

### 5.3 条件分支流转

- 当前流程模型为线性串行节点。
- 尚未支持按金额、组织、业务类型、审批结果等条件路由。

### 5.4 会签/并签/加签/转办

- 当前节点仅支持单个审批人。
- 尚未支持多人会签。
- 尚未支持加签。
- 尚未支持转办到其他审批人但保持流程结构。

### 5.5 流程实例恢复能力

- 当前支持终止、驳回、重建当前待办。
- 尚未支持恢复已驳回流程。
- 尚未支持恢复已终止流程。

### 5.6 流程定义管理增强

- 当前只支持创建草稿和发布。
- 尚未支持复制流程定义。
- 尚未支持停用版本。
- 尚未支持版本回滚。
- 尚未支持流程模板可视化设计器。

### 5.7 更复杂的审批策略

- 当前节点只支持静态审批人。
- 尚未支持按岗位、角色、组织负责人、发起人上级、规则表达式自动算审批人。

---

## 6. P1 级生产化与安全未实现项

### 6.1 全链路签名统一化

- 当前仅部分内部回调具备签名校验。
- 尚未做到所有跨服务请求统一签名、时间戳、防重放保护。

### 6.2 幂等审计完善

- 当前已有部分幂等能力，但未形成完整事件追踪体系。
- 尚未统一记录幂等键、事件号、处理结果、重试次数。

### 6.3 操作审计增强

- 当前更多是业务日志或流程历史。
- 尚未形成完整审计模型，如请求来源、操作 IP、客户端、失败原因归档、调用链追踪等。

### 6.4 监控与告警

- 没有可视化监控面板。
- 没有待办堆积、回调失败率、超时任务数、流程平均耗时等指标。
- 没有自动告警机制。

---

## 7. P2 级前端体验未实现项

### 7.1 待办详情结构化展示

- 当前业务载荷主要以 JSON 原样展示。
- 尚未按业务类型做结构化卡片渲染。

建议补充：

- 薪资审批任务显示批次号、月份、人数、汇总金额、状态。
- 后续其他业务类型按各自字段定制展示区域。

### 7.2 更完整筛选条件

- 当前筛选条件仍偏基础。
- 尚未支持按创建时间范围、优先级、发起人、审批节点、完成时间范围筛选。

### 7.3 列表服务端分页与排序

- 当前列表更偏轻量实现。
- 尚未补齐标准服务端分页、排序和大数据量场景处理。

### 7.4 流程详情增强

- 当前流程详情已可看基本历史。
- 尚未展示 SLA、回调状态、失败回调、节点耗时统计、流程变量快照等信息。

---

## 8. P2 级业务域接入未实现项

### 8.1 其他业务模块统一接入流程中心

当前重点只打通了薪资审批场景，以下业务尚未正式接入统一流程：

- 工时审批
- 请假审批
- 报销审批
- 调岗审批
- 入离职审批
- 排班调整审批
- 考勤异常审批

### 8.2 统一业务载荷契约标准化

- 当前 `taskTypeCode`、`businessType`、`sourcePayload` 已可用，但尚未形成完整标准。
- 不同业务模块接入前，建议先统一契约规范。

建议补充：

- 统一命名规范。
- 必填字段规范。
- 扩展字段规范。
- 版本兼容策略。

---

## 9. 推荐开发顺序

建议按以下顺序推进：

1. `MySQL` 持久化
2. 回调失败重试与补偿
3. 超时调度与自动催办/升级
4. 批量操作结果回显优化
5. 代理规则编辑/停用/删除
6. 批量转交与批量催办
7. 流程撤回与退回上一节点
8. 条件分支、会签、加签等复杂流程能力
9. 其他业务模块统一接入

---

## 10. 建议的下一步

如果按“能上线”优先，建议先做：

- 持久化
- 回调重试
- 补偿机制
- 监控告警

如果按“页面可见能力”优先，建议先做：

- 批量操作结果弹窗
- 代理规则管理增强
- 批量转交
- 流程撤回

---

## 11. 结论

当前 `TodoCenter` 与 `ProcessCenter` 已完成最小可运行闭环，并已覆盖薪资审批主链路。但距离正式生产可用状态，仍需重点补齐以下几类能力：

- 数据持久化
- 失败重试与补偿
- 超时调度
- 正式通知与监控
- 更丰富的待办和流程操作能力
- 其他业务模块接入

该清单可作为后续迭代开发、任务拆分和阶段验收的基础文档。
