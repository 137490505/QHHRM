# 流程中心实例详情页设计说明

## 一、文档目标

本文档用于定义当前项目“流程中心实例详情页”的页面目标、展示方式、交互逻辑和后续实现建议。

本页面的核心目的不是只展示审批记录列表，而是让用户能够直观看到：

- 当前流程实例正在走哪一条审批链路
- 当前流程走到了哪个节点
- 哪些节点已经完成
- 哪些节点还未处理
- 是否发生了驳回、终止、跳过等异常分支

其中最核心的要求是：

- 以流程图形式展示审批流程
- 当前所在节点高亮显示

## 二、建设背景

当前项目已经具备流程中心、流程定义、流程设计器、流程实例、回调日志等基础能力。

随着审批场景逐渐增多，单纯依靠表格、状态字段或时间线文字说明，用户无法快速理解流程当前运行到哪里，也不便于定位审批卡点。

因此建议在当前项目流程中心内补充“流程实例详情页”，对任意审批流程实例提供标准化可视化查看能力。

该页面后续可统一服务于以下场景：

- 供应商新增三方员工审批
- 薪资审批
- 资料变更审批
- 合作伙伴资料审批
- 其他流程中心接入的业务审批

## 三、页面定位

### 1. 页面名称

建议页面名称统一为：

- 流程实例详情页

### 2. 页面入口

建议保留以下入口方式：

- 从流程中心实例列表点击实例名称进入
- 从待办中心点击任务跳转进入
- 从业务单据详情页点击“查看流程”进入
- 从审批记录页点击实例编号进入

### 3. 页面目标

页面目标分为三层：

- 第一层：快速知道当前流程状态和当前节点
- 第二层：直观看懂完整流程走向和已执行路径
- 第三层：查看节点处理记录、审批意见、处理时间和回调信息

## 四、核心需求

### 1. 流程图展示

流程实例详情页必须以流程图方式展示流程定义，不建议只使用步骤条或纯时间线替代。

推荐使用 BPMN 风格或接近 BPMN 的标准图形表达方式，原因如下：

- 与现有流程设计器的输出更统一
- 更容易表达网关、并行、条件分支、驳回回退等复杂场景
- 可直接标记当前节点、已走路径和异常节点
- 用户理解成本更低

### 2. 当前节点高亮

当前流程所在节点必须高亮显示，这是页面最核心的视觉重点。

建议高亮规则如下：

- 当前节点使用蓝色主高亮态
- 节点边框加粗或加外发光
- 节点标题可增加“当前”标识
- 当前节点对应连线可同步高亮

### 3. 节点状态区分

建议将流程图中的节点状态统一分为以下几类：

- 已完成节点
- 当前节点
- 待处理节点
- 已跳过节点
- 驳回节点
- 终止节点

建议视觉规则如下：

- 已完成节点：绿色成功态
- 当前节点：蓝色高亮态
- 待处理节点：灰色默认态
- 已跳过节点：浅灰弱化态
- 驳回节点：红色警示态
- 终止节点：深灰或红灰终止态

### 4. 路径状态展示

除节点外，连线也建议体现状态：

- 已流转路径：高亮显示
- 未流转路径：默认浅色
- 驳回路径：警示色
- 当前路径：与当前节点保持一致的高亮色

### 5. 节点详情查看

点击流程图中的任意节点后，应支持查看节点详情。

建议展示以下信息：

- 节点名称
- 节点类型
- 处理人或候选人
- 节点状态
- 到达时间
- 完成时间
- 审批意见
- 驳回原因

## 五、建议的页面结构

建议整体采用“顶部摘要 + 中部流程图 + 侧边或底部明细”的结构。

### 1. 顶部摘要区

建议展示：

- 流程名称
- 实例编号
- 业务单号
- 发起人
- 发起时间
- 当前状态
- 当前节点
- 流程耗时

### 2. 中部流程图区

流程图区是页面主体，应具备以下能力：

- 加载完整流程定义图
- 根据实例运行态动态渲染节点状态
- 高亮当前节点
- 高亮已流转路径
- 支持缩放
- 支持拖拽
- 支持自适应画布

### 3. 右侧信息区或底部信息区

建议展示以下一个或多个模块：

- 审批时间线
- 节点处理记录
- 审批意见列表
- 回调日志
- 实例基础字段

如果页面空间较大，推荐双栏布局：

- 左侧：流程图
- 右侧：节点详情、审批记录、回调日志

如果页面空间有限，可采用上下布局：

- 上方：流程摘要
- 中间：流程图
- 下方：审批记录和日志

## 六、建议的页面模块拆分

建议实例详情页拆为以下模块，便于后续复用与维护。

### 1. 实例摘要卡片

作用：

- 汇总展示流程基础信息和当前运行状态

建议字段：

- 流程名称
- 实例 ID
- 业务 ID
- 当前状态
- 当前节点名称
- 发起人
- 发起时间

### 2. 流程图查看器

作用：

- 图形化展示流程定义与实例运行状态

建议能力：

- BPMN 图加载
- 节点状态渲染
- 当前节点高亮
- 缩放
- 全屏
- 适配不同屏幕

### 3. 审批时间线

作用：

- 以时间顺序展示审批流转过程

建议字段：

- 节点名称
- 处理人
- 处理动作
- 处理时间
- 审批意见

### 4. 节点详情面板

作用：

- 点击节点后展示该节点的详细处理信息

### 5. 回调日志面板

作用：

- 展示流程实例在业务回调过程中的日志与状态

该模块对于排查流程状态与业务单状态不一致问题很有价值。

## 七、建议的交互逻辑

### 1. 页面初始化

页面加载时，建议按以下顺序获取数据：

1. 获取流程实例基础信息
2. 获取流程定义或 BPMN XML
3. 获取当前节点和历史节点状态
4. 获取审批时间线
5. 获取回调日志
6. 渲染流程图并高亮当前节点

### 2. 节点点击交互

用户点击节点后，建议页面执行：

1. 高亮选中节点
2. 在右侧或下方展示该节点处理详情
3. 如果该节点尚未处理，则展示候选处理人或待处理状态

### 3. 刷新逻辑

建议支持手动刷新，必要时可支持轮询刷新。

适用场景：

- 审批人正在处理当前任务
- 用户希望实时查看当前节点是否推进
- 回调日志可能正在更新

### 4. 异常状态展示

如果流程实例存在异常，应提供显式提示：

- 流程已驳回
- 流程已终止
- 回调失败
- 当前节点数据缺失
- 流程定义与实例状态不一致

## 八、建议的状态映射规则

为了保证前后端展示统一，建议在页面层建立标准状态映射。

### 1. 实例状态

建议实例状态至少支持：

- Draft
- Running
- Approved
- Rejected
- Cancelled
- Completed
- Terminated

### 2. 节点状态

建议节点状态至少支持：

- Completed
- Current
- Pending
- Skipped
- Rejected
- Terminated

### 3. 前端展示文案

建议统一映射为中文展示：

- 已完成
- 当前处理
- 待处理
- 已跳过
- 已驳回
- 已终止

## 九、与现有项目能力的衔接建议

### 1. 与流程设计器衔接

流程实例详情页应优先复用流程设计器已保存的 BPMN 数据或节点数据。

建议实现原则：

- 如果已有 BPMN XML，则直接渲染 BPMN 图
- 如果当前仅有节点结构数据，则通过已有转换逻辑重建图形
- 流程定义变更后，实例详情仍应基于实例对应版本展示

### 2. 与流程中心衔接

建议在当前流程中心模块下增加以下入口：

- 实例列表 -> 查看详情
- 实例详情 -> 流程图标签页
- 实例详情 -> 回调日志标签页

### 3. 与待办中心衔接

待办中心中的审批任务建议统一支持跳转到流程实例详情页。

这样可以实现：

- 从待办直接看到当前流程所在节点
- 审批前先理解上下游流程位置
- 减少只看单条待办带来的上下文缺失

### 4. 与业务单据衔接

后续业务单据详情页也建议统一支持“查看流程”入口，例如：

- 员工资料申请单
- 薪资审批单
- 合作伙伴资料变更单

## 十、推荐的实现优先级

建议分两步实施。

### 第一阶段：先实现可查看版本

目标：

- 能看到流程图
- 能高亮当前节点
- 能查看审批时间线

建议能力：

- 实例详情页
- BPMN 图渲染
- 当前节点高亮
- 审批记录列表

### 第二阶段：再补交互增强

目标：

- 增强可用性和排障能力

建议能力：

- 节点点击详情
- 回调日志联动
- 轮询刷新
- 全屏查看
- 缩放与自适应
- 驳回路径高亮

## 十一、推荐的页面名称与路由建议

建议页面命名如下：

- 中文名称：流程实例详情
- 路由建议：`/workflow/processes/instances/:id`

如果当前项目已有既定路由风格，可按现有流程中心风格统一调整，但建议保留“实例详情”这一语义，避免和“流程定义详情”混淆。

## 十二、初步结论

当前项目流程中心非常有必要补充“流程实例详情页”，并以流程图方式展示审批链路。

其中最核心的建设要求如下：

- 页面必须展示完整流程图
- 当前所在节点必须高亮显示
- 已完成、待处理、驳回、终止等节点状态必须可视区分
- 审批记录和流程图必须联动展示
- 页面应设计成通用能力，供所有审批流程复用

## 十三、后续可继续细化的内容

正式进入开发前，还可以继续补充以下文档：

- 页面原型草图
- 前端组件拆分设计
- 实例详情接口设计
- 节点状态映射字段设计
- 流程图高亮渲染规则说明
- BPMN 数据与实例节点状态的关联方案

---

当前建议优先级：

- 高优先级：实例详情页、流程图展示、当前节点高亮
- 中优先级：审批时间线、节点详情面板、回调日志联动
- 后续增强：全屏、轮询、驳回路径高亮、异常态联动
