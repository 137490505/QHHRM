# 薪资系统第一版开发任务清单

## 1. 目标说明

本清单基于当前 HRMS 现状整理，目标是以 **最小破坏、可分阶段上线** 的方式，完成第一版可落地的薪资系统建设。

本次范围以以下内容为核心：

- 薪资主数据配置化
- 考勤/计件/调整项标准化输入
- 月度薪资计算、审核、发放
- 薪资明细与汇总报表
- 为后续银行代发、员工工资条、OA/考勤机集成预留扩展能力

状态说明（基于当前仓库代码盘点）：

- `已完成`：仓库中已有对应代码、模型、接口或页面实现
- `部分完成`：已有部分基础代码、兼容能力或页面雏形，但未形成完整闭环
- `未完成`：当前仓库中未发现对应落地实现

---

## 2. 建议迭代顺序

### 阶段一：数据库与领域建模

目标：

- 先把薪资域核心表建起来
- 不直接推翻现有 `employee`、`timesheet`、`salary_calculation`
- 为后续 API 和页面提供稳定数据基础

任务：

1. `部分完成` 执行 `sql/HRMS_Payroll_V1_Schema.sql`
   - 已产出 `sql/HRMS_Payroll_V1_Schema.sql`，是否已执行到具体数据库环境需以部署环境为准
2. `已完成` 新增领域实体：
   - `EmployeeType`
   - `EmployeePayrollProfile`
   - `SalaryRule`
   - `OvertimeRateConfig`
   - `HolidayRule`
   - `Attendance`
   - `SalaryAdjustment`
   - `IncomeTaxRule`
   - `PayrollRun`
   - `Payroll`
   - `PayrollDetail`
3. `已完成` 在 `HrmsDbContext` 中注册新表映射
4. `已完成` 新增仓储接口和基础查询仓储
5. `未完成` 准备初始化种子数据：
   - 管理层
   - 自主员工-时薪
   - 自主员工-计件
   - 自主员工-底薪+计件
   - 三方人员

交付物：

- `已完成` MySQL 建表 SQL
- `已完成` .NET 实体和 DbContext 映射
- `已完成` 基础仓储代码

优先级：`P0`

---

### 阶段二：薪资规则配置

目标：

- 把当前硬编码规则改为后台配置驱动
- 让系统管理员和 HR 可维护薪资规则

任务：

1. `部分完成` 员工类型管理
   - 已新增后端查询、新增、编辑接口，前端页面和专用权限点未完成
   - 维护类型名称、薪资模式、是否计加班、是否有饭补/夜补/绩效/社保
2. `部分完成` 薪资规则管理
   - 已新增后端查询、新增、编辑接口，前端页面和完整规则校验未完成
   - 固定薪资
   - 时薪
   - 计件单价
   - 底薪
   - 饭补
   - 夜补
   - 绩效基数
   - 默认加班倍数
3. `未完成` 加班费率配置
   - 工作日
   - 周末
   - 法定节假日
   - 配置有效期
4. `未完成` 节假日规则配置
   - 法定节假日
   - 公司特殊调休日
   - 节假日加班倍数优先覆盖
5. `未完成` 个税规则配置
   - 起征点
   - 税率档
   - 速算扣除数

交付物：

- `部分完成` 后端配置 API
- `未完成` 前端配置页面
- `未完成` 基础权限点

优先级：`P0`

---

### 阶段三：薪资数据输入

目标：

- 统一月度算薪输入来源
- 支持批量导入和人工修正

任务：

1. `部分完成` 考勤数据管理
   - 已有旧版 `timesheet` 列表、导入、审批链路，并新增正式 `attendance` 查询、新增、编辑 API；前端页面未完成
   - 日考勤维护
   - 正常工时
   - 加班工时
   - 加班类型
   - 夜班标识
2. `部分完成` 计件数据管理
   - 数据模型已预留 `attendance.piecework_qty`，管理能力未完整落地
   - 在 `attendance` 中使用 `piecework_qty`
   - 或后续拆成独立计件表
3. `部分完成` 薪资调整项管理
   - 调整项实体和表结构已定义，并新增查询、新增、编辑 API；前端页面未完成
   - 社保个人
   - 社保公司
   - 公积金个人
   - 公积金公司
   - 个税
   - 餐补
   - 夜补
   - 绩效奖金
   - 违纪扣款
   - 其他补贴
   - 其他扣款
4. `部分完成` Excel 导入能力
   - 已支持工时兼容导入，员工薪资档案和调整项导入未完成
   - 员工薪资档案导入
   - 考勤导入
   - 调整项导入
5. `部分完成` 导入校验
   - 已具备基础员工/组织匹配校验，模板校验、重复校验细化和错误明细导出未完整落地
   - 模板校验
   - 员工匹配
   - 重复数据检查
   - 错误明细导出

交付物：

- `部分完成` 数据录入 API
  - 已新增员工薪资档案、考勤、薪资调整项后端 API，导入接口与前端录入页面未完成
- `未完成` Excel 模板
- `部分完成` 导入页面和校验结果页面

优先级：`P1`

---

### 阶段四：薪资计算引擎

目标：

- 建立正式月结流程
- 按员工类型和薪资模式自动算薪

任务：

1. `未完成` 新增月度算薪服务 `PayrollCalculationService`
2. `部分完成` 实现 5 类计算逻辑：
   - 已有旧版 `SalaryCalculationService`，但正式五类薪资模型未按新薪资域完整实现
   - 管理层
   - 自主员工-时薪
   - 自主员工-计件
   - 自主员工-底薪+计件
   - 三方人员
3. `未完成` 实现加班倍数判定优先级：
   - `holiday_rule`
   - `overtime_rate_config`
   - `salary_rule.default_overtime_multiplier`
4. `未完成` 汇总调整项
5. `未完成` 生成 `payroll_run`
6. `未完成` 生成 `payroll`
7. `未完成` 生成 `payroll_detail`
8. `未完成` 支持单人试算
9. `部分完成` 支持全月批量核算
   - 旧版 `SalaryCalculationService` 已支持批量核算，正式 `payroll` 批次核算未完成
10. `未完成` 支持重新计算与回滚

交付物：

- `部分完成` 计算服务
- `未完成` 月结接口
- `未完成` 试算接口
- `未完成` 回滚接口

优先级：`P0`

---

### 阶段五：审核、发放、工资条

目标：

- 建立完整薪资状态流转
- 满足 HR 审核与财务发放场景

任务：

1. `部分完成` 状态流转设计
   - 实体和页面交互中已出现状态字段与审批/发放动作，但正式后端流转未闭环
   - `Draft`
   - `Calculated`
   - `Approved`
   - `Paid`
   - `RolledBack`
2. `部分完成` 月结审核页面
3. `部分完成` 单人薪资明细查看
4. `未完成` 工资条导出
   - Excel
   - PDF
5. `未完成` 银行代发导出
   - 预留不同银行模板
6. `未完成` 发放记录维护

交付物：

- `未完成` 审核 API
- `未完成` 发放 API
- `部分完成` 工资条页面
- `未完成` 导出功能

优先级：`P1`

---

### 阶段六：报表与成本分析

目标：

- 提供管理层与财务需要的分析结果

任务：

1. `部分完成` 员工工资条查询
2. `未完成` 部门薪资汇总
3. `未完成` 社保/公积金公司成本汇总
4. `未完成` 加班成本分析
5. `部分完成` 三方人员薪资与甲方账单联动分析
   - 已有 `ThirdPartyBillService`，但尚未与正式 `payroll` 域打通
6. `未完成` 月度人工成本看板

交付物：

- `未完成` 报表 API
- `未完成` 汇总页面
- `未完成` 导出能力

优先级：`P2`

---

## 3. 技术实现建议

### 后端建议

1. 新增薪资域服务：
   - `EmployeeTypeService`
   - `SalaryRuleService`
   - `AttendanceService`
   - `SalaryAdjustmentService`
   - `PayrollCalculationService`
   - `PayrollRunService`
   - `PayrollReportService`
2. 把当前 `SalaryCalculationService` 逐步降级为兼容层，后续迁移到正式 `payroll`
3. 计算逻辑建议拆分为：
   - 规则读取器
   - 员工分类器
   - 计算器
   - 汇总器
   - 明细生成器
4. 回滚不要直接删除历史数据，建议通过 `payroll_run` 和状态做版本留痕

### 前端建议

1. 新增页面：
   - 员工类型管理
   - 薪资规则管理
   - 加班费率配置
   - 节假日管理
   - 考勤数据管理
   - 调整项管理
   - 月度算薪
   - 薪资审核
   - 工资条查询
   - 薪资报表
2. 导入场景优先做 Excel 模板 + 结果回显
3. 月结页面必须支持：
   - 试算
   - 正式核算
   - 回滚
   - 审核
   - 发放

---

## 4. 推荐优先开发包

### 开发包 A：底座建设

- 建表 SQL
- 实体
- DbContext
- 仓储
- 初始化数据

目标：让系统“存得下”

### 开发包 B：规则配置

- 员工类型
- 薪资规则
- 加班费率
- 节假日
- 个税规则

目标：让系统“配得动”

### 开发包 C：数据输入与算薪

- 考勤
- 调整项
- Excel 导入
- 试算
- 正式核算

目标：让系统“算得出”

### 开发包 D：审核与报表

- 薪资审核
- 工资条
- 导出
- 报表

目标：让系统“用得起来”

---

## 5. 建议工期

按 1 名后端 + 1 名前端 + 1 名测试的最小配置估算：

1. 底座建设：5 到 7 个工作日
2. 规则配置：5 到 7 个工作日
3. 数据输入与算薪：7 到 10 个工作日
4. 审核与报表：5 到 7 个工作日
5. 联调与修复：5 个工作日

总计建议：`27 到 36 个工作日`

---

## 6. 当前最推荐的下一步

1. 先执行 `HRMS_Payroll_V1_Schema.sql`
2. 再生成后端实体与 `DbContext` 映射
3. 然后优先做：
   - 员工类型管理
   - 薪资规则管理
   - 月度算薪服务骨架
4. 最后再推进前端月结页面和导入页面

---

## 7. 第一版验收标准

### 7.1 功能验收标准

第一版上线时，至少应满足以下验收项：

1. 能维护员工类型、薪资规则、加班费率、节假日规则、个税规则
2. 能为员工建立薪资档案，并支持查询与变更留痕
3. 能按月导入或录入考勤数据、计件数据、薪资调整项
4. 能对导入数据进行基础校验，并输出错误明细
5. 能按员工类型自动完成试算与正式核算
6. 能生成月度 `payroll_run`、员工 `payroll`、明细 `payroll_detail`
7. 能支持查看薪资明细、月度汇总、审核状态和发放状态
8. 能支持重新计算与回滚，且保留历史批次记录
9. 能导出工资条和基础薪资汇总报表

### 7.2 数据验收标准

1. 同一员工、同一月度、同一薪资批次不能重复生成正式工资单
2. 员工薪资结果必须可追溯到原始输入数据和规则快照
3. 回滚后原批次数据不物理删除，需保留状态与操作记录
4. 薪资明细中应能区分：
   - 基本工资
   - 时薪工资
   - 计件工资
   - 加班工资
   - 津贴补贴
   - 应扣项目
   - 个税
   - 实发工资
5. 报表汇总数据应与薪资单明细可对账

### 7.3 性能验收建议

以第一版目标规模，建议按以下基线验收：

1. 单月 500 名员工批量算薪可在 3 分钟内完成
2. 单人试算接口响应时间建议控制在 3 秒内
3. 导入 5000 条考勤或调整项数据时，页面能返回校验结果且不超时
4. 报表导出在常规数据量下可在 1 分钟内完成

---

## 8. 角色分工建议

### 8.1 业务角色

1. HR
   - 维护员工薪资档案
   - 录入或导入考勤、调整项
   - 发起月度试算和正式核算
   - 查看工资条和异常结果
2. 财务
   - 审核薪资结果
   - 维护发放状态
   - 导出银行代发文件
   - 核对个税、社保、公积金等金额
3. 系统管理员
   - 维护规则配置
   - 控制权限
   - 处理基础参数和初始化数据

### 8.2 开发分工建议

1. 后端
   - 数据模型
   - 配置 API
   - 导入校验
   - 算薪引擎
   - 审核与发放接口
   - 报表接口
2. 前端
   - 规则配置页面
   - 导入页面
   - 月结页面
   - 审核与工资条页面
   - 报表页面
3. 测试
   - 规则配置测试
   - 导入校验测试
   - 算薪准确性测试
   - 回滚与重算测试
   - 审核发放流程测试

---

## 9. 测试范围建议

### 9.1 核心测试场景

1. 管理层固定工资计算正确
2. 时薪员工按正常工时和加班工时计算正确
3. 计件员工按计件数量和单价计算正确
4. 底薪加计件员工可正确汇总两类收入
5. 三方人员按约定方式计算，不混入内部员工规则
6. 节假日、周末、工作日加班倍数优先级生效正确
7. 调整项增减后，实发工资计算正确
8. 个税、社保、公积金扣减正确
9. 重复导入、重复核算、重复发放被正确拦截
10. 回滚后可重新试算和重新正式核算

### 9.2 边界测试场景

1. 员工月中入职
2. 员工月中离职
3. 员工类型月中变更
4. 同一员工存在多类调整项
5. 跨月加班或跨天夜班
6. 节假日与调休日冲突
7. 无考勤数据但有固定薪资
8. 有考勤数据但缺少薪资档案
9. 员工被停用或删除后的历史薪资查看

### 9.3 建议输出物

1. 算薪测试案例表
2. 导入模板样例数据
3. 员工类型计算对照表
4. 月结流程回归测试清单

---

## 10. 风险与依赖

### 10.1 主要依赖项

1. 组织、员工、岗位基础数据完整
2. 现有 `employee`、`timesheet`、`salary_calculation` 可被兼容使用
3. 财务或 HR 能提供现行薪资规则口径
4. 个税、社保、公积金口径有明确制度依据
5. Excel 导入模板字段提前冻结

### 10.2 主要风险点

1. 现有薪资规则存在口头约定，未完全文档化
2. 历史考勤和计件数据格式不统一，导入清洗成本高
3. 员工分类口径不稳定，可能导致规则配置频繁返工
4. 试算结果与人工表格口径不一致时，容易造成上线阻塞
5. 回滚策略如果设计不当，可能影响审计和历史追踪

### 10.3 风险应对建议

1. 在开发前先整理一份“薪资规则确认表”，由 HR 和财务共同签字确认
2. 第一版先固定 5 类员工计算模型，避免过早做成无限可配置规则引擎
3. 保留人工调整项入口，用于承接暂时无法自动化的口径
4. 每次正式月结前，先跑试算并与手工工资表抽样对比
5. 所有关键操作保留操作人、操作时间、操作原因

---

## 11. 上线准备清单

### 11.1 数据准备

1. 初始化员工类型
2. 初始化薪资规则
3. 初始化加班费率
4. 初始化节假日规则
5. 初始化个税规则
6. 导入员工薪资档案
7. 准备首月考勤、计件、调整项样例数据

### 11.2 权限准备

1. 配置薪资规则管理权限
2. 配置考勤与调整项导入权限
3. 配置算薪、审核、发放权限
4. 配置报表导出权限
5. 配置敏感薪资数据查看权限

### 11.3 上线前检查

1. 核对生产环境规则参数
2. 核对员工薪资档案是否完整
3. 核对导入模板字段是否与系统一致
4. 核对试算结果与手工工资表差异
5. 完成关键流程演练：
   - 导入
   - 试算
   - 正式核算
   - 审核
   - 发放
   - 导出

---

## 12. 第二版可扩展方向

在第一版落地后，可继续扩展以下能力：

1. 银行代发文件按银行模板配置化
2. 员工自助工资条查询
3. 与 OA 审批联动的薪资调整流程
4. 与排班系统、考勤机、钉钉、企微做正式接口集成
5. 社保、公积金自动分摊与成本归集
6. 项目部、产线、班组维度的人力成本分析
7. 历史薪资版本比对和差异审计
8. 薪资异常预警与自动校验规则

---

## 13. 最终建议结论

如果当前目标是“尽快让薪资系统第一版落地”，最合理的推进方式是：

1. 先完成底座建设和规则配置，确保模型和口径稳定
2. 再完成导入、试算、正式核算，打通月结主流程
3. 最后补齐审核、发放、工资条和报表，形成完整闭环
4. 第一版尽量坚持“有限规则配置 + 人工调整兜底”的原则，优先保证可用性与准确性
5. 待第一版稳定运行后，再逐步推进银行代发、自助工资条、外部系统集成等增强能力

---

## 14. 后端接口清单建议

### 14.1 员工类型与规则配置

建议优先提供以下接口：

1. `GET /api/payroll/employee-types`
2. `POST /api/payroll/employee-types`
3. `PUT /api/payroll/employee-types/{id}`
4. `GET /api/payroll/salary-rules`
5. `POST /api/payroll/salary-rules`
6. `PUT /api/payroll/salary-rules/{id}`
7. `GET /api/payroll/overtime-rate-configs`
8. `POST /api/payroll/overtime-rate-configs`
9. `PUT /api/payroll/overtime-rate-configs/{id}`
10. `GET /api/payroll/holiday-rules`
11. `POST /api/payroll/holiday-rules`
12. `PUT /api/payroll/holiday-rules/{id}`
13. `GET /api/payroll/income-tax-rules`
14. `POST /api/payroll/income-tax-rules`
15. `PUT /api/payroll/income-tax-rules/{id}`

### 14.2 员工薪资档案

1. `GET /api/payroll/profiles`
2. `GET /api/payroll/profiles/{employeeId}`
3. `POST /api/payroll/profiles`
4. `PUT /api/payroll/profiles/{id}`
5. `POST /api/payroll/profiles/import`
6. `GET /api/payroll/profiles/import-template`

### 14.3 考勤与调整项

1. `GET /api/payroll/attendance`
2. `POST /api/payroll/attendance`
3. `PUT /api/payroll/attendance/{id}`
4. `POST /api/payroll/attendance/import`
5. `GET /api/payroll/attendance/import-template`
6. `GET /api/payroll/adjustments`
7. `POST /api/payroll/adjustments`
8. `PUT /api/payroll/adjustments/{id}`
9. `POST /api/payroll/adjustments/import`
10. `GET /api/payroll/adjustments/import-template`

### 14.4 算薪与月结

1. `POST /api/payroll/runs/trial`
2. `POST /api/payroll/runs/calculate`
3. `POST /api/payroll/runs/{runId}/rollback`
4. `GET /api/payroll/runs`
5. `GET /api/payroll/runs/{runId}`
6. `GET /api/payroll/runs/{runId}/employees/{employeeId}`
7. `POST /api/payroll/runs/{runId}/approve`
8. `POST /api/payroll/runs/{runId}/pay`

### 14.5 报表与导出

1. `GET /api/payroll/reports/summary`
2. `GET /api/payroll/reports/department-summary`
3. `GET /api/payroll/reports/payslips`
4. `GET /api/payroll/export/payslips`
5. `GET /api/payroll/export/bank-file`

---

## 15. 前端页面清单建议

### 15.1 配置类页面

1. 员工类型管理
2. 薪资规则管理
3. 加班费率配置
4. 节假日规则管理
5. 个税规则管理
6. 员工薪资档案管理

### 15.2 数据输入类页面

1. 考勤数据列表
2. 考勤导入页面
3. 调整项列表
4. 调整项导入页面
5. 导入结果回显页面

### 15.3 月结与审核类页面

1. 月度算薪页面
2. 单人试算抽屉或弹窗
3. 薪资批次详情页面
4. 薪资审核页面
5. 发放记录页面

### 15.4 查询与报表类页面

1. 工资条查询页面
2. 薪资汇总报表页面
3. 部门薪资汇总页面
4. 成本分析页面

---

## 16. 权限点建议

为避免薪资敏感信息越权查看，建议至少拆分以下权限：

1. `payroll.employeeType.view`
2. `payroll.employeeType.edit`
3. `payroll.salaryRule.view`
4. `payroll.salaryRule.edit`
5. `payroll.profile.view`
6. `payroll.profile.edit`
7. `payroll.attendance.import`
8. `payroll.adjustment.import`
9. `payroll.run.trial`
10. `payroll.run.calculate`
11. `payroll.run.rollback`
12. `payroll.run.approve`
13. `payroll.run.pay`
14. `payroll.report.view`
15. `payroll.export.payslip`
16. `payroll.export.bank`

权限控制建议：

1. HR 可配置基础资料、导入数据、发起试算
2. 财务可查看结果、审核、发放、导出银行文件
3. 业务主管默认不直接查看全员实发工资，仅看授权范围报表
4. 超级管理员仅用于初始化和异常处理，不建议参与日常薪资操作

---

## 17. 数据字典与枚举建议

### 17.1 员工类型枚举建议

1. `Management`
2. `Hourly`
3. `Piecework`
4. `BasePlusPiecework`
5. `ThirdParty`

### 17.2 薪资批次状态建议

1. `Draft`
2. `Calculated`
3. `Approved`
4. `Paid`
5. `RolledBack`

### 17.3 调整项类型建议

1. `Allowance`
2. `Deduction`
3. `SocialSecurityPersonal`
4. `SocialSecurityCompany`
5. `HousingFundPersonal`
6. `HousingFundCompany`
7. `IncomeTax`
8. `MealAllowance`
9. `NightShiftAllowance`
10. `PerformanceBonus`
11. `DisciplinaryDeduction`
12. `Other`

### 17.4 加班类型建议

1. `Workday`
2. `Weekend`
3. `Holiday`

---

## 18. 开发落地顺序建议

### 18.1 第一步：先打通后端底座

1. 建表 SQL
2. 实体与枚举
3. `DbContext` 映射
4. 仓储层与查询接口
5. 初始化种子数据

### 18.2 第二步：再做规则配置

1. 员工类型管理 API
2. 薪资规则管理 API
3. 加班费率管理 API
4. 节假日规则管理 API
5. 个税规则管理 API

### 18.3 第三步：打通算薪主链路

1. 员工薪资档案
2. 考勤与调整项输入
3. 单人试算
4. 全月正式核算
5. 薪资明细生成
6. 回滚机制

### 18.4 第四步：补齐业务闭环

1. 审核
2. 发放
3. 工资条
4. 报表
5. 导出

---

## 19. 建议里程碑

### 19.1 里程碑 M1：底座完成

完成标准：

1. 所有核心表已建成
2. 后端实体已可正常查询写入
3. 基础种子数据已初始化
4. 规则配置 API 可联调

### 19.2 里程碑 M2：规则可配

完成标准：

1. 员工类型可维护
2. 薪资规则可维护
3. 加班费率、节假日、个税规则可维护
4. 前端配置页面可用

### 19.3 里程碑 M3：月结可跑

完成标准：

1. 可导入考勤和调整项
2. 可完成单人试算
3. 可完成整月正式核算
4. 可查看个人薪资明细

### 19.4 里程碑 M4：流程闭环

完成标准：

1. 可审核
2. 可发放
3. 可导出工资条
4. 可查看基础汇总报表

---

## 20. 推荐目录规划

结合当前项目结构，建议新增如下模块目录：

### 20.1 后端建议

1. `src/HRMS.Domain/Entities/Payroll`
2. `src/HRMS.Domain/Enums/Payroll`
3. `src/HRMS.Application/Contracts/Payroll`
4. `src/HRMS.Application/Services/Payroll`
5. `src/HRMS.Infrastructure/Persistence/Configurations/Payroll`
6. `src/HRMS.API/Controllers/Payroll`

### 20.2 前端建议

1. `hrms-web/src/views/payroll`
2. `hrms-web/src/api/payroll.js`
3. `hrms-web/src/components/payroll`
4. `hrms-web/src/utils/payroll`

---

文档结束。
